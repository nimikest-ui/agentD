# Lean pinned dependency set for Termux (Android / bionic libc).
#
# This is the LEAN HEADLESS CORE: the agent loop + HTTP providers (Xiaomi/mimo,
# Kimi) + the Claude CLI provider. Deliberately ABSENT:
#   * the interactive-TUI engine `deepagents-cli` and its server stack
#     (langgraph-cli[inmem] -> langgraph-api -> grpcio / grpcio-tools). grpcio's
#     pip sdist does not build on bionic, and Termux's prebuilt grpcio (1.81.0) is
#     out of langgraph-api's `grpcio<1.81.0` pin. It's an opt-in `[tui]`/`[full]`
#     extra for glibc desktops — see docs/TERMUX.md.
#   * the browser stack (browser-use, playwright, cdp-use, bubus, pyee) — glibc
#     Chromium can't run on bionic.
#   * sqlite-vec / langgraph-checkpoint-sqlite (no Android wheel). For persistent
#     SQLite checkpointing install it separately, skipping the vector-store dep:
#       pip install --no-deps 'langgraph-checkpoint-sqlite>=3.1.0'
#     Without it, persistence.py falls back to in-memory checkpointing.
#
# PREFERRED install path is `pip install -e .` (pip-resolved lean core). Use this
# file only when you want exact pinned reproducibility; it was generated from a
# reference resolve, so versions may float slightly against your Python minor.
# Providers covered: Xiaomi/mimo, Kimi (HTTP) and claude-cli (needs the Node
# `claude` binary).
#
aiosqlite==0.22.1
annotated-types==0.7.0
anthropic==0.105.2
anyio==4.13.0
bracex==2.6
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
cryptography==48.0.0
deepagents==0.6.7
distro==1.9.0
docstring_parser==0.18.0
filetype==1.2.0
google-auth==2.53.0
google-genai==2.7.0
greenlet==3.5.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.17
jiter==0.15.0
jsonpatch==1.33
jsonpointer==3.1.1
langchain==1.3.2
langchain-anthropic==1.4.4
langchain-core==1.4.0
langchain-google-genai==4.2.4
langchain-protocol==0.0.16
langgraph==1.2.2
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.3.15
langsmith==0.8.8
linkify-it-py==2.1.0
markdown-it-py==4.2.0
mdit-py-plugins==0.6.1
mdurl==0.1.2
ollama==0.6.2
openai==2.40.0
orjson==3.11.9
ormsgpack==1.12.2
packaging==26.2
platformdirs==4.10.0
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
PyYAML==6.0.3
requests==2.34.2
requests-toolbelt==1.0.0
rich==15.0.0
sniffio==1.3.1
SQLAlchemy==2.0.50
tenacity==9.1.4
textual==8.2.7
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
uc-micro-py==2.0.0
urllib3==2.7.0
uuid_utils==0.16.0
wcmatch==10.1
websockets==16.0
xxhash==3.7.0
zstandard==0.25.0
